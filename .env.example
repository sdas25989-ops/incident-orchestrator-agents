# ── ServiceNow ──────────────────────────────────────────────────────────────
# Your ServiceNow instance URL (no trailing slash)
SERVICENOW_INSTANCE=https://your-instance.service-now.com

# ServiceNow agent account with read/write access to incidents
SN_USER=your_sn_username
SN_PASS=your_sn_password

# Exact name of the assignment group that owns the incidents
SN_GROUP=Your Assignment Group Name

# API field name for the "Problem Correlation Code (PCC)" field on the incident form.
# To find it: open any incident → right-click the PCC field label → "Show Field Name".
SN_PCC_FIELD=u_problem_correlation_code

# ── Anthropic / Claude ───────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── Order Cancellation API ───────────────────────────────────────────────────
# Base URL of the internal Order Management REST API
# Leave as http://localhost:9999 to run in stub/mock mode
ORDER_API_BASE_URL=https://internal-order-api.company.com

# Bearer token for the Order Management API
ORDER_API_KEY=your_order_api_key

# ── Orchestrator Settings ────────────────────────────────────────────────────
# Name written to the "assigned_to" field when resolving incidents
ENGINEER_NAME=Incident Orchestrator Bot

# How often (in seconds) to poll ServiceNow for new incidents
POLL_INTERVAL_SECONDS=60
